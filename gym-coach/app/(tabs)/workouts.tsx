import { View, Text } from "react-native";

export default function WorkoutsScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ color: "#fff" }}>Workouts</Text>
    </View>
  );
}
